diff --git a/data/api.py b/data/api.py
index 00df7d8..8660513 100644
--- a/data/api.py
+++ b/data/api.py
@@ -531,16 +531,19 @@ def get_all_bdr():
         if _bdr_cache is not None and (now - _bdr_cache_ts) < _CACHE_TTL:
             return CompactJSONResponse(content=_bdr_cache)
     try:
-        result = None
+        pg_result = {}
+        fb_result = {}
         if pg_available():
             try:
-                result = get_live_bdr()
+                pg_result = get_live_bdr() or {}
             except Exception as e:
                 print(f"[API] DB query failed in get_all_bdr, falling back to files: {e}")
-        if not result:
-            result = get_live_bdr_fallback()
-        if not result:
-            result = {}
+        try:
+            fb_result = get_live_bdr_fallback() or {}
+        except Exception as e:
+            print(f"[API] File fallback failed in get_all_bdr: {e}")
+        result = dict(fb_result)
+        result.update(pg_result)
         with _cache_lock:
             _bdr_cache = result
             _bdr_cache_ts = time.time()
@@ -581,16 +584,19 @@ def get_all_rings():
         if _rings_cache is not None and (now - _rings_cache_ts) < _CACHE_TTL:
             return CompactJSONResponse(content=_rings_cache)
     try:
-        result = None
+        pg_result = {}
+        fb_result = {}
         if pg_available():
             try:
-                result = get_live_rings()
+                pg_result = get_live_rings() or {}
             except Exception as e:
                 print(f"[API] DB query failed in get_all_rings, falling back to files: {e}")
-        if not result:
-            result = get_live_rings_fallback()
-        if not result:
-            result = {}
+        try:
+            fb_result = get_live_rings_fallback() or {}
+        except Exception as e:
+            print(f"[API] File fallback failed in get_all_rings: {e}")
+        result = dict(fb_result)
+        result.update(pg_result)
         with _cache_lock:
             _rings_cache = result
             _rings_cache_ts = time.time()
diff --git a/data/main.py b/data/main.py
index c39737f..1d255e9 100644
--- a/data/main.py
+++ b/data/main.py
@@ -52,7 +52,7 @@ ARCHIVE_INTERVAL = 30          # 30 seconds between BDR snapshots
 _last_archive_time = 0.0        # last archive timestamp (module-level tracker)
 _archive_migrated = False       # one-time flag: old flat date folders → machine/date layout
 CONFIG_FILE = _PARENT / "machines.json"
-DEFAULT_DESTINATION = r"C:\Users\meshe\Music\BDR DASHBOARD\AQC 47"
+DEFAULT_DESTINATION = r"D:\BDR\DESTINATION"
 
 DEFAULT_MACHINES = [
     {"name": "aqc-47", "user": "aqc-47", "ip": "172.16.18.71"},
diff --git a/dump_json.py b/dump_json.py
deleted file mode 100644
index 303477d..0000000
--- a/dump_json.py
+++ /dev/null
@@ -1,13 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM live_bdr_raw WHERE machine_name = %s', ('aqc-28',))
-    row = cur.fetchone()
-    data = json.loads(row['content'])
-    for k, v in data.get('slots', {}).items():
-        if str(v.get('serial_number')).lower() == serial:
-            with open('full_slot.json', 'w') as f:
-                json.dump(v, f, indent=2)
diff --git a/ecosystem.config.cjs b/ecosystem.config.cjs
index d5521c2..cb0ea2a 100644
--- a/ecosystem.config.cjs
+++ b/ecosystem.config.cjs
@@ -1,17 +1,37 @@
-﻿module.exports = {
-  apps: [{
-    name: 'bdr-api',
-    script: 'D:\\BDR\\data\\main.py',
+﻿const apps = [{
+  name: 'bdr-api',
+  script: 'D:\\BDR\\data\\main.py',
+  interpreter: 'C:\\Users\\meshe\\AppData\\Local\\Programs\\Python\\Python312\\python.exe',
+  args: '--bg-api --start-pg',
+  restart_delay: 10000,
+  kill_timeout: 10000,
+  merge_logs: true,
+  min_uptime: 30000,
+  autorestart: true,
+  env: {
+    AQC_PASSWORD: '1234',
+    NO_STARTUP_INDEX: '1'
+  }
+}];
+
+const BIC_ENABLED = process.env.BIC_ENABLED === '1';
+
+if (BIC_ENABLED) {
+  apps.push({
+    name: 'bic',
+    script: 'D:\\BDR\\bic\\main.py',
     interpreter: 'C:\\Users\\meshe\\AppData\\Local\\Programs\\Python\\Python312\\python.exe',
-    args: '--bg-api --start-pg',
+    cwd: 'D:\\BDR',
+    args: '',
     restart_delay: 10000,
     kill_timeout: 10000,
     merge_logs: true,
     min_uptime: 30000,
     autorestart: true,
     env: {
-      AQC_PASSWORD: '1234',
-      NO_STARTUP_INDEX: '1'
+      PYTHONPATH: 'D:\\BDR'
     }
-  }]
-};
+  });
+}
+
+module.exports = { apps };
diff --git a/find_amazing.py b/find_amazing.py
deleted file mode 100644
index f069819..0000000
--- a/find_amazing.py
+++ /dev/null
@@ -1,12 +0,0 @@
-﻿import sys, os, json
-from pathlib import Path
-p = Path('D:/BDR/DESTINATION/archive')
-files = list(p.rglob('*.json'))
-for f in files:
-    try:
-        with open(f, 'r', encoding='utf-8') as fd:
-            content = fd.read()
-            if '12.04' in content and '0000038' in content:
-                print(f'FOUND AMAZING FILE: {f}')
-    except:
-        pass
diff --git a/find_func.py b/find_func.py
deleted file mode 100644
index a2fa93c..0000000
--- a/find_func.py
+++ /dev/null
@@ -1,6 +0,0 @@
-﻿import ast
-with open('D:/BDR/data/api.py', 'r') as f:
-    lines = f.readlines()
-for i, line in enumerate(lines):
-    if '_calc_avg_bdr' in line:
-        print(f'{i+1}: {line.strip()}')
diff --git a/fix_db.py b/fix_db.py
deleted file mode 100644
index 5bc1999..0000000
--- a/fix_db.py
+++ /dev/null
@@ -1,22 +0,0 @@
-﻿import sys, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-conn = get_connection()
-with conn.cursor() as cur:
-    cur.execute('''
-        UPDATE archive_entries
-        SET machine_avg_bdr = (
-            SELECT avg(cast(value->>'bdr' as float))
-            FROM jsonb_array_elements(
-                case 
-                    when content->'bdr_state'->'completed_cycles' is not null 
-                    then content->'bdr_state'->'completed_cycles' 
-                    else '[]'::jsonb 
-                end
-            )
-        )
-        WHERE content->'bdr_state'->'completed_cycles' is not null 
-          AND jsonb_array_length(content->'bdr_state'->'completed_cycles') > 0
-    ''')
-    conn.commit()
-print('Fixed avg_bdr for historical records.')
diff --git a/fix_db2.py b/fix_db2.py
deleted file mode 100644
index 9438be7..0000000
--- a/fix_db2.py
+++ /dev/null
@@ -1,23 +0,0 @@
-﻿import sys, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-conn = get_connection()
-conn.autocommit = True
-with conn.cursor() as cur:
-    cur.execute("SET statement_timeout = 0;")
-    cur.execute('''
-        UPDATE archive_entries
-        SET machine_avg_bdr = (
-            SELECT avg(cast(value->>'bdr' as float))
-            FROM jsonb_array_elements(
-                case 
-                    when content->'bdr_state'->'completed_cycles' is not null 
-                    then content->'bdr_state'->'completed_cycles' 
-                    else '[]'::jsonb 
-                end
-            )
-        )
-        WHERE content->'bdr_state'->'completed_cycles' is not null 
-          AND jsonb_array_length(content->'bdr_state'->'completed_cycles') > 0
-    ''')
-print('Fixed avg_bdr for historical records.')
diff --git a/machines.json b/machines.json
index 951060e..87e4887 100644
--- a/machines.json
+++ b/machines.json
@@ -45,7 +45,11 @@
       "name": "aqc-05",
       "user": "aqc-05",
       "ip": "172.16.18.42",
-      "removed_slots": [1, 23, 60]
+      "removed_slots": [
+        1,
+        23,
+        60
+      ]
     },
     {
       "name": "aqc-06",
@@ -211,6 +215,36 @@
       "name": "aqc-44",
       "user": "aqc-44",
       "ip": "172.16.18.60"
+    },
+    {
+      "name": "aqc-51",
+      "user": "aqc-51",
+      "ip": "172.16.18.127"
+    },
+    {
+      "name": "aqc-53",
+      "user": "aqc-53",
+      "ip": "172.16.18.132"
+    },
+    {
+      "name": "aqc-49",
+      "user": "aqc-49",
+      "ip": "172.16.18.126"
+    },
+    {
+      "name": "aqc-29",
+      "user": "aqc-29",
+      "ip": "172.16.18.229"
+    },
+    {
+      "name": "aqc-50",
+      "user": "aqc-50",
+      "ip": "172.16.18.128"
+    },
+    {
+      "name": "aqc-52",
+      "user": "aqc-52",
+      "ip": "172.16.18.133"
     }
   ],
   "destination_bdr": "D:\\BDR\\DESTINATION",
diff --git a/read_match.py b/read_match.py
deleted file mode 100644
index 0b339ce..0000000
--- a/read_match.py
+++ /dev/null
@@ -1,8 +0,0 @@
-﻿import json
-with open('D:/BDR/DESTINATION/archive/aqc-23/2026-07-08/01-50-17.json', 'r') as fd:
-    data = json.load(fd)
-    for k, v in data.get('slots', {}).items():
-        if '0000038' in str(v.get('serial_number')):
-            print('SERIAL:', v.get('serial_number'))
-            print('STATE:', v.get('state'))
-            print(json.dumps(v.get('bdr_state'), indent=2))
diff --git a/search_aqc28.py b/search_aqc28.py
deleted file mode 100644
index ebd5172..0000000
--- a/search_aqc28.py
+++ /dev/null
@@ -1,13 +0,0 @@
-﻿import sys, os, json
-from pathlib import Path
-p = Path('D:/BDR/DESTINATION/archive/aqc-28')
-files = list(p.rglob('*.json'))
-for f in files:
-    try:
-        with open(f, 'r', encoding='utf-8') as fd:
-            content = fd.read()
-            if '12.04' in content and '0000038' in content:
-                print(f'FOUND: {f}')
-    except:
-        pass
-print('Done searching aqc-28')
diff --git a/search_multi.py b/search_multi.py
deleted file mode 100644
index 227448c..0000000
--- a/search_multi.py
+++ /dev/null
@@ -1,24 +0,0 @@
-﻿import sys, os
-from multiprocessing import Pool
-from pathlib import Path
-
-def check_file(f):
-    try:
-        with open(f, 'r', encoding='utf-8') as fd:
-            content = fd.read()
-            # The serial we are looking for: RA-CH3-LFK-W1-AG08-0000038
-            if 'RA-CH3-LFK-W1-AG08-0000038' in content and '12.04' in content:
-                return str(f)
-    except:
-        pass
-    return None
-
-if __name__ == '__main__':
-    p = Path('D:/BDR/DESTINATION/archive')
-    files = list(p.rglob('*.json'))
-    with Pool(8) as pool:
-        for result in pool.imap_unordered(check_file, files, chunksize=1000):
-            if result:
-                print(f'MATCH FOUND: {result}')
-                sys.exit(0)
-    print('No match found.')
diff --git a/search_names.py b/search_names.py
deleted file mode 100644
index 380b49d..0000000
--- a/search_names.py
+++ /dev/null
@@ -1,11 +0,0 @@
-﻿import os
-from pathlib import Path
-
-p = Path('D:/BDR/DESTINATION/archive')
-files = list(p.rglob('*0000038*'))
-for f in files:
-    print(f'FOUND FILE BY NAME: {f}')
-files2 = list(p.rglob('*12.04*'))
-for f in files2:
-    print(f'FOUND FILE BY NAME: {f}')
-print('Done searching file names')
diff --git a/search_precise.py b/search_precise.py
deleted file mode 100644
index 5775b71..0000000
--- a/search_precise.py
+++ /dev/null
@@ -1,33 +0,0 @@
-﻿import sys, os, json
-from multiprocessing import Pool
-from pathlib import Path
-
-def check_file(f):
-    try:
-        with open(f, 'r', encoding='utf-8') as fd:
-            content = fd.read()
-            if '0000038' not in content:
-                return None
-            data = json.loads(content)
-            for k, v in data.get('slots', {}).items():
-                if '0000038' in str(v.get('serial_number')).upper():
-                    bdr_state = v.get('bdr_state', {})
-                    cycles = bdr_state.get('completed_cycles', [])
-                    if len(cycles) == 6:
-                        return f'MATCH 6 CYCLES: {f}'
-                    for c in cycles:
-                        if abs(c.get('bdr', 0) - 12.04) < 0.1:
-                            return f'MATCH 12.04 BDR: {f}'
-    except:
-        pass
-    return None
-
-if __name__ == '__main__':
-    p = Path('D:/BDR/DESTINATION/archive')
-    files = list(p.rglob('*.json'))
-    with Pool(8) as pool:
-        for result in pool.imap_unordered(check_file, files, chunksize=1000):
-            if result:
-                print(result)
-                sys.exit(0)
-    print('No match found.')
diff --git a/test_1204.py b/test_1204.py
deleted file mode 100644
index 38472d6..0000000
--- a/test_1204.py
+++ /dev/null
@@ -1,18 +0,0 @@
-﻿import sys, psycopg2, psycopg2.extras, json
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM archive_entries')
-    rows = cur.fetchall()
-    for row in rows:
-        data = row['content']
-        for k, v in data.get('slots', {}).items():
-            bdr_state = v.get('bdr_state', {})
-            cycles = bdr_state.get('completed_cycles', [])
-            if len(cycles) == 6:
-                total = sum([c.get('bdr', 0) for c in cycles])
-                if abs(total / 6.0 - 12.04) < 0.1:
-                    print(f"FOUND 12.04 AVG BDR ON SERIAL: {v.get('serial_number')}")
-                    sys.exit(0)
-    print("No 6-cycle ring with 12.04 avg BDR found.")
diff --git a/test_ag06.py b/test_ag06.py
deleted file mode 100644
index 2ccfd87..0000000
--- a/test_ag06.py
+++ /dev/null
@@ -1,17 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag06-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM live_bdr_raw WHERE machine_name = %s', ('aqc-15',))
-    row = cur.fetchone()
-    data = json.loads(row['content'])
-    for k, v in data.get('slots', {}).items():
-        if str(v.get('serial_number')).lower() == serial:
-            from api import _count_workouts_from_completed_cycles, _calc_avg_bdr
-            cycles = v.get('bdr_state', {}).get('completed_cycles', [])
-            print(f'Cycles: {len(cycles)}')
-            avg, est = _calc_avg_bdr(v)
-            print(f'Avg BDR: {avg}')
-            print(json.dumps(cycles, indent=2))
diff --git a/test_all_db.py b/test_all_db.py
deleted file mode 100644
index a061cb9..0000000
--- a/test_all_db.py
+++ /dev/null
@@ -1,19 +0,0 @@
-﻿import sys, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM archive_entries WHERE serial_lower = %s', (serial,))
-    rows = cur.fetchall()
-    found = False
-    for row in rows:
-        rec = row['content']
-        bdr = rec.get('avg_bdr')
-        cycles = rec.get('completed_cycles', 0)
-        state = rec.get('state')
-        if bdr is not None or cycles > 5 or state != 'BDR_RUNNING':
-            print(f'Machine: {rec.get("machine")} | State: {state} | Cycles: {cycles} | BDR: {bdr} | saved_at: {rec.get("saved_at")}')
-            found = True
-    if not found:
-        print(f'No valid rows found in {len(rows)} records for {serial}')
diff --git a/test_all_machines.py b/test_all_machines.py
deleted file mode 100644
index 6467e83..0000000
--- a/test_all_machines.py
+++ /dev/null
@@ -1,17 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, content FROM live_bdr_raw')
-    rows = cur.fetchall()
-    for row in rows:
-        machine = row['machine_name']
-        data = json.loads(row['content'])
-        for k, v in data.get('slots', {}).items():
-            if serial in str(v.get('serial_number')):
-                print(f'LIVE Machine: {machine}')
-                bdr_state = v.get('bdr_state', {})
-                cycles = bdr_state.get('completed_cycles', [])
-                print(f'State: {v.get("state")} | Cycles: {len(cycles)} | Cycle: {bdr_state.get("cycle")}')
diff --git a/test_any_recent.py b/test_any_recent.py
deleted file mode 100644
index beab47a..0000000
--- a/test_any_recent.py
+++ /dev/null
@@ -1,7 +0,0 @@
-﻿import sys, os
-from pathlib import Path
-p = Path('D:/BDR/DESTINATION/archive')
-files = [f for f in p.rglob('*') if f.is_file()]
-files.sort(key=lambda x: os.path.getmtime(x), reverse=True)
-for f in files[:20]:
-    print(f'{f} - {os.path.getmtime(f)}')
diff --git a/test_arch.py b/test_arch.py
deleted file mode 100644
index e98d1b5..0000000
--- a/test_arch.py
+++ /dev/null
@@ -1,5 +0,0 @@
-﻿import sys, json, os
-from pathlib import Path
-sys.path.append('d:/BDR/data')
-import api
-print('Archive Root:', api._get_archive_root())
diff --git a/test_calc.py b/test_calc.py
deleted file mode 100644
index 3ec20ef..0000000
--- a/test_calc.py
+++ /dev/null
@@ -1,14 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, content FROM live_bdr_raw WHERE machine_name = %s', ('aqc-28',))
-    row = cur.fetchone()
-    data = json.loads(row['content'])
-    for k, v in data.get('slots', {}).items():
-        if serial in str(v.get('serial_number')):
-            from api import _calc_avg_bdr
-            avg, est = _calc_avg_bdr(v)
-            print(f'Computed BDR: {avg} | Est: {est}')
diff --git a/test_calc_fix.py b/test_calc_fix.py
deleted file mode 100644
index b7ef6c4..0000000
--- a/test_calc_fix.py
+++ /dev/null
@@ -1,17 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, content FROM live_bdr_raw WHERE machine_name = %s', ('aqc-28',))
-    row = cur.fetchone()
-    data = json.loads(row['content'])
-    for k, v in data.get('slots', {}).items():
-        if serial in str(v.get('serial_number')):
-            completed = v.get("bdr_state", {}).get("completed_cycles", [])
-            print(f'completed_cycles length: {len(completed)}')
-            values = [c.get("bdr") for c in completed if c.get("bdr") is not None]
-            print(f'BDR values: {values}')
-            if values:
-                print(f'Avg: {sum(values) / len(values)}')
diff --git a/test_db_6.py b/test_db_6.py
deleted file mode 100644
index d5e4381..0000000
--- a/test_db_6.py
+++ /dev/null
@@ -1,14 +0,0 @@
-﻿import sys, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM archive_entries WHERE serial_lower = %s ORDER BY saved_at DESC', (serial,))
-    rows = cur.fetchall()
-    for i, row in enumerate(rows):
-        rec = row['content']
-        cycles = rec.get('completed_cycles', 0)
-        bdr = rec.get('avg_bdr')
-        if cycles == 6 or (bdr is not None and abs(bdr - 12.04) < 0.1):
-            print(f'FOUND: {rec.get("saved_at")} | State: {rec.get("state")} | Cycles: {cycles} | BDR: {bdr}')
diff --git a/test_dest_json.py b/test_dest_json.py
deleted file mode 100644
index 04b41a3..0000000
--- a/test_dest_json.py
+++ /dev/null
@@ -1,20 +0,0 @@
-﻿import sys, json
-from pathlib import Path
-sys.path.append('d:/BDR/data')
-from api import _calc_avg_bdr
-p = Path('D:/BDR/DESTINATION/archive')
-files = list(p.rglob('*.json'))
-found = False
-for f in files:
-    with open(f) as fd:
-        data = json.load(fd)
-        for k, v in data.get('slots', {}).items():
-            if '0000038' in str(v.get('serial_number')):
-                avg, _ = _calc_avg_bdr(v)
-                cycles = v.get('bdr_state', {}).get('completed_cycles', [])
-                if avg is not None or len(cycles) > 5:
-                    print(f'File: {f}')
-                    print(f'BDR: {avg} | Cycles: {len(cycles)} | State: {v.get("state")}')
-                    found = True
-if not found:
-    print('Not found in any JSON file!')
diff --git a/test_exact_json.py b/test_exact_json.py
deleted file mode 100644
index e4941bb..0000000
--- a/test_exact_json.py
+++ /dev/null
@@ -1,8 +0,0 @@
-﻿import json
-with open('D:/BDR/DESTINATION/archive/aqc-28/2026-07-10/07-15-17.json', 'r') as fd:
-    data = json.load(fd)
-    for k, v in data.get('slots', {}).items():
-        if '0000038' in str(v.get('serial_number')):
-            print('SERIAL:', v.get('serial_number'))
-            print('STATE:', v.get('state'))
-            print(json.dumps(v.get('bdr_state'), indent=2))
diff --git a/test_exact_serial.py b/test_exact_serial.py
deleted file mode 100644
index f132609..0000000
--- a/test_exact_serial.py
+++ /dev/null
@@ -1,19 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, downloaded_at, content FROM live_bdr_raw')
-    rows = cur.fetchall()
-    for row in rows:
-        machine = row['machine_name']
-        data = json.loads(row['content'])
-        for k, v in data.get('slots', {}).items():
-            if str(v.get('serial_number')).lower() == serial:
-                print(f'LIVE Machine: {machine}')
-                bdr_state = v.get('bdr_state', {})
-                cycles = bdr_state.get('completed_cycles', [])
-                print(f'State: {v.get("state")} | Cycles: {len(cycles)} | Last Update: {row["downloaded_at"]}')
-                print('RAW JSON for this slot:')
-                print(json.dumps(v, indent=2))
diff --git a/test_filepath.py b/test_filepath.py
deleted file mode 100644
index e2d2023..0000000
--- a/test_filepath.py
+++ /dev/null
@@ -1,9 +0,0 @@
-﻿import sys, psycopg2, psycopg2.extras, json
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM archive_entries WHERE serial_lower = %s LIMIT 1', (serial,))
-    row = cur.fetchone()
-    print(row['content'].get('file_path'))
diff --git a/test_json.py b/test_json.py
deleted file mode 100644
index f7ad18d..0000000
--- a/test_json.py
+++ /dev/null
@@ -1,20 +0,0 @@
-﻿import sys, json
-from pathlib import Path
-sys.path.append('d:/BDR/data')
-from api import _calc_avg_bdr
-p = Path('d:/BDR/data/archive')
-files = list(p.rglob('*.json'))
-found = False
-for f in files:
-    with open(f) as fd:
-        data = json.load(fd)
-        for k, v in data.get('slots', {}).items():
-            if '0000038' in str(v.get('serial_number')):
-                avg, _ = _calc_avg_bdr(v)
-                cycles = v.get('bdr_state', {}).get('completed_cycles', [])
-                if avg is not None or len(cycles) > 5:
-                    print(f'File: {f}')
-                    print(f'BDR: {avg} | Cycles: {len(cycles)} | State: {v.get("state")}')
-                    found = True
-if not found:
-    print('Not found in any JSON file!')
diff --git a/test_live.py b/test_live.py
deleted file mode 100644
index 01a4b34..0000000
--- a/test_live.py
+++ /dev/null
@@ -1,20 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, content FROM live_bdr_raw')
-    rows = cur.fetchall()
-    for row in rows:
-        machine = row['machine_name']
-        data = json.loads(row['content'])
-        for k, v in data.get('slots', {}).items():
-            if serial in str(v.get('serial_number')):
-                print(f'LIVE Machine: {machine}')
-                bdr_state = v.get('bdr_state', {})
-                cycles = bdr_state.get('completed_cycles', [])
-                print(f'State: {v.get("state")} | Cycles: {len(cycles)}')
-                from api import _calc_avg_bdr
-                avg, _ = _calc_avg_bdr(v)
-                print(f'Avg BDR: {avg}')
diff --git a/test_live_time.py b/test_live_time.py
deleted file mode 100644
index e6439cb..0000000
--- a/test_live_time.py
+++ /dev/null
@@ -1,17 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, downloaded_at, content FROM live_bdr_raw')
-    rows = cur.fetchall()
-    for row in rows:
-        machine = row['machine_name']
-        data = json.loads(row['content'])
-        for k, v in data.get('slots', {}).items():
-            if serial in str(v.get('serial_number')):
-                print(f'LIVE Machine: {machine}')
-                bdr_state = v.get('bdr_state', {})
-                cycles = bdr_state.get('completed_cycles', [])
-                print(f'State: {v.get("state")} | Cycles: {len(cycles)} | Last Update: {row["downloaded_at"]}')
diff --git a/test_newest.py b/test_newest.py
deleted file mode 100644
index 5c1f2ec..0000000
--- a/test_newest.py
+++ /dev/null
@@ -1,14 +0,0 @@
-﻿import sys, os, json
-from pathlib import Path
-p = Path('D:/BDR/DESTINATION/archive')
-files = list(p.rglob('*.json'))
-files.sort(key=lambda x: os.path.getmtime(x), reverse=True)
-for f in files[:50]:
-    with open(f, 'r') as fd:
-        content = fd.read()
-        if '0000038' in content:
-            print(f'FOUND IN: {f}')
-            data = json.loads(content)
-            for k, v in data.get('slots', {}).items():
-                if '0000038' in str(v.get('serial_number')):
-                    print(json.dumps(v.get('bdr_state'), indent=2))
diff --git a/test_pm2.bat b/test_pm2.bat
deleted file mode 100644
index e69de29..0000000
diff --git a/test_raw.py b/test_raw.py
deleted file mode 100644
index dd61daf..0000000
--- a/test_raw.py
+++ /dev/null
@@ -1,13 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, content FROM live_bdr_raw WHERE machine_name = %s', ('aqc-28',))
-    row = cur.fetchone()
-    data = json.loads(row['content'])
-    for k, v in data.get('slots', {}).items():
-        if serial in str(v.get('serial_number')):
-            print('RAW DATA:')
-            print(json.dumps(v, indent=2))
diff --git a/test_recent.py b/test_recent.py
deleted file mode 100644
index 653b4c8..0000000
--- a/test_recent.py
+++ /dev/null
@@ -1,7 +0,0 @@
-﻿import sys, os
-from pathlib import Path
-p = Path('D:/BDR/DESTINATION/archive')
-files = list(p.rglob('*.json'))
-files.sort(key=lambda x: os.path.getmtime(x), reverse=True)
-for f in files[:10]:
-    print(f"{f} - {os.path.getmtime(f)}")
diff --git a/test_rings.py b/test_rings.py
deleted file mode 100644
index e871c0b..0000000
--- a/test_rings.py
+++ /dev/null
@@ -1,14 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = '0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT machine_name, content FROM live_rings_raw')
-    rows = cur.fetchall()
-    for row in rows:
-        data = json.loads(row['content'])
-        for k, v in data.items():
-            if serial in str(v.get('serial_number')):
-                print(f'RINGS Machine: {row["machine_name"]} | Slot: {k}')
-                print(json.dumps(v, indent=2))
diff --git a/test_search_api.py b/test_search_api.py
deleted file mode 100644
index f909ed6..0000000
--- a/test_search_api.py
+++ /dev/null
@@ -1,5 +0,0 @@
-﻿import requests
-import json
-res = requests.get('http://localhost:3000/api/bdr/search_old_data?serial_number=ra-ch3-lfk-w1-rt08-0000161')
-print(res.status_code)
-print(res.text)
diff --git a/test_search_api2.py b/test_search_api2.py
deleted file mode 100644
index a9b29a8..0000000
--- a/test_search_api2.py
+++ /dev/null
@@ -1,8 +0,0 @@
-﻿import requests
-import json
-try:
-    res = requests.get('http://localhost:8000/api/bdr/search_old_data?serial_number=ra-ch3-lfk-w1-rt08-0000161')
-    print(res.status_code)
-    print(res.text[:500])
-except Exception as e:
-    print(e)
diff --git a/test_search_api3.py b/test_search_api3.py
deleted file mode 100644
index cd70c4b..0000000
--- a/test_search_api3.py
+++ /dev/null
@@ -1,8 +0,0 @@
-﻿import requests
-import json
-try:
-    res = requests.get('http://localhost:8000/api/old-data/search?serial=RA-CH3-LFK-W1-AG08-0000038')
-    print(res.status_code)
-    print(res.text[:500])
-except Exception as e:
-    print(e)
diff --git a/test_search_api4.py b/test_search_api4.py
deleted file mode 100644
index aa24280..0000000
--- a/test_search_api4.py
+++ /dev/null
@@ -1,8 +0,0 @@
-﻿import requests
-import json
-try:
-    res = requests.post('http://localhost:8000/api/old-data/search', json={'serials': ['RA-CH3-LFK-W1-AG08-0000038']})
-    print(res.status_code)
-    print(res.text[:500])
-except Exception as e:
-    print(e)
diff --git a/test_search_api5.py b/test_search_api5.py
deleted file mode 100644
index e37bd1c..0000000
--- a/test_search_api5.py
+++ /dev/null
@@ -1,8 +0,0 @@
-﻿import requests
-import json
-try:
-    res = requests.get('http://localhost:8000/api/old-data/search/RA-CH3-LFK-W1-AG08-0000038')
-    print(res.status_code)
-    print(res.text[:500])
-except Exception as e:
-    print(e)
diff --git a/test_search_api6.py b/test_search_api6.py
deleted file mode 100644
index 54036c3..0000000
--- a/test_search_api6.py
+++ /dev/null
@@ -1,13 +0,0 @@
-﻿import requests
-import json
-import time
-
-time.sleep(2)
-try:
-    res = requests.get('http://localhost:8000/api/old-data/search/RA-CH3-LFK-W1-AG08-0000038')
-    print(res.status_code)
-    data = res.json()
-    if 'results' in data and len(data['results']) > 0:
-        print('latest avg_bdr:', data['results'][0]['avg_bdr'])
-except Exception as e:
-    print(e)
diff --git a/test_search_api7.py b/test_search_api7.py
deleted file mode 100644
index 8eaa235..0000000
--- a/test_search_api7.py
+++ /dev/null
@@ -1,9 +0,0 @@
-﻿import requests
-import json
-try:
-    res = requests.get('http://localhost:8000/api/old-data/search/RA-CH3-LFK-W1-AG08-0000038')
-    data = res.json()
-    if 'results' in data and len(data['results']) > 0:
-        print('latest avg_bdr:', data['results'][0]['avg_bdr'])
-except Exception as e:
-    print(e)
diff --git a/test_serial.py b/test_serial.py
deleted file mode 100644
index 615dcf7..0000000
--- a/test_serial.py
+++ /dev/null
@@ -1,16 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM archive_entries WHERE serial_lower = %s ORDER BY saved_at DESC', (serial,))
-    rows = cur.fetchall()
-    valid = []
-    for i, row in enumerate(rows):
-        rec = row['content']
-        if rec.get('avg_bdr') is not None or rec.get('completed_cycles', 0) > 0 or rec.get('state') != 'BDR_RUNNING':
-            valid.append(rec)
-    print(f'Found {len(valid)} valid rows out of {len(rows)}.')
-    for rec in valid:
-        print(f'saved_at: {rec.get("saved_at")} | state: {rec.get("state")} | cycles: {rec.get("completed_cycles")} | bdr: {rec.get("avg_bdr")} | machine: {rec.get("machine")}')
diff --git a/test_slot.py b/test_slot.py
deleted file mode 100644
index d22e1dd..0000000
--- a/test_slot.py
+++ /dev/null
@@ -1,7 +0,0 @@
-﻿import json
-with open('D:/BDR/DESTINATION/archive/aqc-23/2026-07-08/01-50-17.json', 'r') as f:
-    data = json.load(f)
-for k, v in data.get('slots', {}).items():
-    if '12.04' in json.dumps(v):
-        print(f"SLOT: {k}, SERIAL: {v.get('serial_number')}")
-        print(json.dumps(v.get('bdr_state'), indent=2))
diff --git a/test_workouts.py b/test_workouts.py
deleted file mode 100644
index e25a55d..0000000
--- a/test_workouts.py
+++ /dev/null
@@ -1,15 +0,0 @@
-﻿import sys, json, psycopg2, psycopg2.extras
-sys.path.append('d:/BDR/data')
-from postgres_db import get_connection
-serial = 'ra-ch3-lfk-w1-ag08-0000038'
-conn = get_connection()
-with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
-    cur.execute('SELECT content FROM live_bdr_raw WHERE machine_name = %s', ('aqc-28',))
-    row = cur.fetchone()
-    data = json.loads(row['content'])
-    for k, v in data.get('slots', {}).items():
-        if str(v.get('serial_number')).lower() == serial:
-            from api import _count_workouts_from_completed_cycles
-            cycles = v.get('bdr_state', {}).get('completed_cycles', [])
-            w = _count_workouts_from_completed_cycles(cycles)
-            print(f'Workouts computed: {w}')
